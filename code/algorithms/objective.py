from ..classes.schedule import Schedule

# TODO: #15 Implement objective function which couples a score to a schedule
def objective(schedule: Schedule) -> float:
    # TODO: first check whether schedule is at all valid (no doubly booked rooms etc.)

    score: float = 0

    return score
